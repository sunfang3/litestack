development:
  adapter: litecable
  # path: storage/development/cable.sqlite3
  # Optional: cross-process notify (requires gem "honker", file-backed path)
  # transport: honker
  # watcher_poll_interval_ms: 5
  # See docs/HONKER.md and samples/litecable.honker.yml

test:
  adapter: test

production:
  adapter: litecable
  # path: storage/production/cable.sqlite3
  # transport: honker
  # leadership: true   # single pruner when Honker locks available
