<section class="lb-empty" aria-labelledby="error-heading">
  <h2 id="error-heading"><%= h(@error_title || "Error") %></h2>
  <p><%= h(@error_message || "An unexpected error occurred.") %></p>
  <p><a href="/">Return to home</a> and verify the metrics database path.</p>
</section>
